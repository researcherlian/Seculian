[build]
builder = "NIXPACKS"

[build.config]
pythonVersion = "3.11"

[deploy]
startCommand = "cd backend && gunicorn flask_app:app --bind 0.0.0.0:$PORT --timeout 120 --workers 2"

[build.env]
PYTHONPATH = "/app/backend"
